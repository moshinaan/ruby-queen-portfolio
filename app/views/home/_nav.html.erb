  <nav class="nav shell" aria-label="Main navigation">
    <a class="logo" href="#top" aria-label="<%= t(".logo_aria") %>"><span>RQ</span> Ruby Queen</a>
    <div class="nav-right">
      <a href="#about"><%= t(".about") %></a>
      <a href="#stack"><%= t(".stack") %></a>
      <a href="#experience"><%= t(".experience") %></a>
      <a href="#projects"><%= t(".projects") %></a>
      <a href="#contact"><%= t(".contact") %></a>
      <div class="language-switcher" aria-label="<%= t(".language_aria") %>">
        <%= link_to "RU", root_path(locale: :ru), class: ("active" if I18n.locale == :ru), aria: { label: t(".russian_aria"), pressed: I18n.locale == :ru } %>
        <%= link_to "EN", root_path(locale: :en), class: ("active" if I18n.locale == :en), aria: { label: t(".english_aria"), pressed: I18n.locale == :en } %>
      </div>
    </div>
  </nav>
